function deportes(persona: string, ...deportes: string[]): string {
    console.log(deportes);
    return `A ${persona} le gustan los siguientes deportes: ${deportes.join(', ')}`
}
deportes('Cristobal', 'Futbol, Tennis, Golf, Baseball');

// ejemplo
function function_ejemplo(requerido, por_default='algo', opcional? ) {

}
